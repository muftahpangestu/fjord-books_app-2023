<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to t('action.book.edit'), edit_book_path(@book) %> |
  <%= link_to t('action.book.back_to_list'), books_path %>

  <%= button_to t('action.book.destroy'), @book, method: :delete %>
</div>
